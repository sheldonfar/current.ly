import Wykop from '../containers/Wykop'
import WykopLogo from '../containers/Wykop/logo.png'

export default {
    wykop: {
        label: 'Wykop',
        logo: WykopLogo,
        component: Wykop,
        route: '/wykop'
    }
}